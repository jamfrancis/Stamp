// EnhancedMapView.swift
// Bridge file for backward compatibility - imports from organized Maps structure

import SwiftUI

// These are now just aliases that point to the organized implementations

// Re-export the interface for backward compatibility
typealias StampInfoPopup = MapKitExpandingPopup